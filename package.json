{
  "name": "z-search",
  "version": "1.0.0",
  "description": "Node.js based search CLI. Use inverted index as the way to search.",
  "main": "./lib/index.js",
  "scripts": {
    "test": "jest --silent",
    "coverage": "jest --silent --coverage",
    "setup": "npm i -g && z-search -h",
    "setup:sudo": "sudo npm i -g && z-search -h"
  },
  "keywords": [
    "cli",
    "search",
    "javascript",
    "node.js"
  ],
  "author": "Someone",
  "license": "ISC",
  "bin": {
    "z-search": "./lib/index.js"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "commander": "^6.2.1",
    "figlet": "^1.5.0",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "jest": {
    "verbose": true,
    "moduleDirectories": [
      "lib"
    ]
  }
}
